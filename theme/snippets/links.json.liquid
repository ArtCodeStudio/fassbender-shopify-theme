[
    {% for link in linklist.links %}
        {% if link.handle == 'blog' and settings.blog_enable == false %}
        {% else %}
            {% include 'link.json' %},
        {% endif %}
    {% endfor %}
]