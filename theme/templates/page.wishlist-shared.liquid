{% comment %} // Uncomment if option_selection.js is missing
{{ 'option_selection.js' | shopify_asset_url | script_tag }}
{% endcomment %}

<div class="ah-container-fluid">
  <div class="container-fluid">
    <div id="wishlist-king" class="row">

        <div class="col-xs-12">
            <h1>{{ page.title }}</h1>
            {{ page.content }}

            <!-- include 'wishlist-shared' -->

        </div>
    </div>
  </div>
</div>