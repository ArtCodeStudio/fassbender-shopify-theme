<div class="container-fluid pt-5" id="activate_account">
  <div class="row pt-md-5">
    <div class="col-12 col-md-10 offset-md-2">

      <h1 class="h6">{{ 'customer.activate_account.title' | t }}</h1>
      <p>{{ 'customer.activate_account.subtext' | t }}</p>

      {% comment %}
        This form must use 'activate_customer_password'
      {% endcomment %}

      {% form 'activate_customer_password' %}

        {% comment %} Error and Successfull messages {% endcomment %}
        {% if form.errors %}
          {% assign formErrors = form.errors %}
        {% endif %}
        {% if form.posted_successfully? %}
          {% assign formSuccessfully = 'Account successfully activated' %}
        {% endif %}

        {% if formErrors %}
          <div class="alert alert-danger" role="alert">
            {{ formErrors | default_errors }}
          </div>
        {% endif %}

        {% if formSuccessfully %}
          <div class="alert alert-success" role="alert">
            {{ formSuccessfully }}
          </div>
        {% endif %}

        <div class="form-group input-group mb-0">
          <label for="CustomerPassword" class="sr-only">{{ 'customer.activate_account.password' | t }}</label>
          <input
            type="password"
            value=""
            name="customer[password]"
            id="CustomerPassword"
            class="form-control form-control-text form-control-primary py-1"
            placeholder="{{ 'customer.activate_account.password' | t }}"
            autocomplete="new-password"
          >
        </div>

        <div class="form-group input-group mb-0">
          <label for="CustomerPasswordConfirmation" class="sr-only">{{ 'customer.activate_account.password_confirm' | t }}</label>
          <input
            type="password"
            value=""
            name="customer[password_confirmation]"
            id="CustomerPasswordConfirmation"
            class="form-control form-control-text form-control-primary py-1"
            placeholder="{{ 'customer.activate_account.password_confirm' | t }}"
            autocomplete="new-password"
          >
        </div>
        <div class="form-group input-group my-3">
          <button type="submit" class="btn btn-animated-underline-dark font-weight-bold px-0">
            {{ 'customer.activate_account.submit' | t }}
          </button>
        </div>

        <div class="form-group input-group my-3">
          <button type="submit" class="btn btn-animated-underline-dark font-weight-bold px-0" name="decline">
            {{ 'customer.activate_account.cancel' | t }}
          </button>
        </div>
      {% endform %}

    </div>
  </div>
</div>