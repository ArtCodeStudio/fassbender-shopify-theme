import { IComponentWrapperResult } from '../../../modules/tinybind/index';
declare global {
    interface Window {
        model: any;
    }
}
/**
 * contact
 */
export declare const contactComponent: () => IComponentWrapperResult<any>;
