import { IComponentWrapperResult } from '../../../modules/tinybind/index';
declare global {
    interface Window {
        model: any;
    }
}
/**
 * nav-items
 */
export declare const iconsetComponent: () => IComponentWrapperResult<any>;
